<%= form_for(@restaurant_tile) do |f| %>
  <% if @restaurant_tile.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@restaurant_tile.errors.count, "error") %> prohibited this restaurant_tile from being saved:</h2>

      <ul>
      <% @restaurant_tile.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.text_field :restaurant_id %>
  </div>
  <div class="field">
      <%= f.file_field :avatar %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
