<%= form_for(@category) do |f| %>
  <% if @category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@category.errors.count, "error") %> prohibited this category from being saved:</h2>

      <ul>
      <% @category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="panel panel-default">
  <div class="panel-heading">
    <h3 class="panel-title">
      <%if @restaurant %>
      New 
      <% else %>
      Editing 
      <% end %>
      Menu Category</h3>
  </div>
  <div class="panel-body">
    <div class="row">
  <div class="field col-md-8">
      <%= f.label :name, 'Category Name' %><br>
      <%= f.text_field :name, :class=>'form-control' %>
    </div>
  <div class="field col-md-4">
  <br>
  <%= f.label :hide%>
  <%= f.check_box :hide %>
</div>
</div>
<br>
<div class="row">
  <div class="field col-md-8">
    <%= f.label :menu_id %><br>
    <%= f.select :menu_id, options_for_select(restaurant_menu_options)%>
  </div>
    <div class="field">
    <%= f.hidden_field :restaurant_id, value: category_menu_id_default_value %>
  </div>
  <br>
  <div class="actions col-md-4 text-right">
    <%= f.submit 'Submit', class: 'btn btn-default btn-xs' %>
  </div>  
</div>




  </div>
  </div>

  
<% end %>
 
